<%= @callback_name %> ( 
	{
		description: '<%= @sale_item.name %>',
		image: '<%= request.protocol + request.host + ":" + request.port.to_s + @sale_item.image.url(:medium)%>',
		buy_url: '<%= url_for buy_shop_sale_item_url(@shop, @sale_item)%>',
		sale_url: '<%= url_for shop_sale_item_url(@shop, @sale_item)%>',
		price: <%= @sale_item.price %>
	} 
)